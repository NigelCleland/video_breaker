============
Installation
============

At the command line::

    $ easy_install video_breaker

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv video_breaker
    $ pip install video_breaker